{
  "name": "marrakechdunes-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx watch index.ts",
    "build": "tsc",
    "start": "cross-env NODE_ENV=production node --experimental-specifier-resolution=node dist/server/index.js"
  },
  "dependencies": {
    "rest-express": "file:.."
  }
}
